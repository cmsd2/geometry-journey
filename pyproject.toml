[project]
name = "geometry-journey"
version = "0.1.0"
description = "A self-contained tour of geometry in Maxima notebooks."
requires-python = ">=3.13"
dependencies = [
    "maxima-nbconvert[plotly]",
]

[tool.uv.sources]
maxima-nbconvert = { path = "../maxima-nbconvert", editable = true }
